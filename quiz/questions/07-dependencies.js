// Module: External dependencies (packages, sources, freshness, exposures, mesh)
QuizBank.add([
  {
    id: "dependencies-001",
    module: "dependencies",
    topic: "Packages",
    topic_fr: "Packages",
    question: "What is the role of the `package-lock.yml` file (dbt 1.7+)?",
    choices: [
      "Listing the packages to install, instead of `packages.yml`",
      "Pinning the exact versions resolved during the last `dbt deps` to guarantee reproducible installs",
      "Preventing `dbt deps` from running in production",
      "Storing credentials for private repositories",
    ],
    answer: [1],
    explanation: "`packages.yml` declares constraints (e.g. `version: [\">=1.1.0\", \"<1.2.0\"]`). `package-lock.yml` records the exact resolved version (or git commit). Subsequent `dbt deps` reuse the lock; `dbt deps --upgrade` regenerates it. This project has one.",
    source: "https://docs.getdbt.com/docs/build/packages#lock-file",
    fr: {
      question: "Quel est le rôle du fichier `package-lock.yml` (dbt 1.7+) ?",
      choices: [
        "Lister les packages à installer, à la place de `packages.yml`",
        "Figer les versions exactes résolues lors du dernier `dbt deps` pour garantir des installations reproductibles",
        "Empêcher l'exécution de `dbt deps` en production",
        "Stocker les credentials des dépôts privés",
      ],
      explanation: "`packages.yml` déclare les contraintes (ex : `version: [\">=1.1.0\", \"<1.2.0\"]`). `package-lock.yml` enregistre la version exacte (ou le commit git) résolue. Les `dbt deps` suivants réutilisent le lock ; `dbt deps --upgrade` le régénère. Ce projet en a un.",
    },
  },
  {
    id: "dependencies-002",
    module: "dependencies",
    topic: "Packages",
    topic_fr: "Packages",
    question: "How do you declare a dependency on `dbt_utils` accepting any 1.3 patch but not 1.4?",
    choices: [
      "`version: 1.3`",
      "`version: [\">=1.3.0\", \"<1.4.0\"]`",
      "`version: ~1.3`",
      "`version: latest-1.3`",
    ],
    answer: [1],
    explanation: "A version range is written as a list of constraints. `version: 1.3.0` would pin an exact version. Hub packages are declared with `package: dbt-labs/dbt_utils`; you can also use `git:` (+ `revision:`) or `local:`.",
    source: "https://docs.getdbt.com/docs/build/packages",
    fr: {
      question: "Comment déclarer une dépendance vers `dbt_utils` acceptant tout patch de la 1.3 mais pas la 1.4 ?",
      choices: [
        "`version: 1.3`",
        "`version: [\">=1.3.0\", \"<1.4.0\"]`",
        "`version: ~1.3`",
        "`version: latest-1.3`",
      ],
      explanation: "Une plage de versions s'écrit comme une liste de contraintes. `version: 1.3.0` épinglerait une version exacte. Les packages du Hub sont déclarés via `package: dbt-labs/dbt_utils` ; on peut aussi utiliser `git:` (+ `revision:`) ou `local:`.",
    },
  },
  {
    id: "dependencies-003",
    module: "dependencies",
    topic: "Source freshness",
    topic_fr: "Source freshness",
    question: "What must be declared on a source for `dbt source freshness` to work? (choose 2)",
    choices: [
      "`loaded_at_field` (load timestamp column)",
      "A `freshness` block with `warn_after` and/or `error_after`",
      "`event_time`",
      "A `not_null` test on the date column",
    ],
    answer: [0, 1],
    explanation: "dbt computes `max(loaded_at_field)` and compares it to the current time. If the gap exceeds `warn_after` / `error_after` (`{count: 12, period: hour}`), the status is warn/error. Results are written to `target/sources.json`. The `freshness` block can be inherited at source level and overridden per table. (`loaded_at_query` is an alternative since 1.10.)",
    source: "https://docs.getdbt.com/docs/build/sources#declaring-source-freshness",
    fr: {
      question: "Que faut-il déclarer sur une source pour que `dbt source freshness` fonctionne ? (2 réponses)",
      choices: [
        "`loaded_at_field` (colonne d'horodatage de chargement)",
        "Un bloc `freshness` avec `warn_after` et/ou `error_after`",
        "`event_time`",
        "Un test `not_null` sur la colonne de date",
      ],
      explanation: "dbt calcule `max(loaded_at_field)` et le compare à l'instant courant. Si l'écart dépasse `warn_after` / `error_after` (`{count: 12, period: hour}`), le statut est warn/error. Les résultats sont écrits dans `target/sources.json`. Le bloc `freshness` peut être hérité au niveau source et surchargé par table. (`loaded_at_query` est une alternative depuis 1.10.)",
    },
  },
  {
    id: "dependencies-004",
    module: "dependencies",
    topic: "Exposures",
    topic_fr: "Exposures",
    question: "Which properties are **required** to declare an exposure? (choose 3)",
    choices: ["name", "type", "owner (name or email)", "url", "depends_on"],
    answer: [0, 1, 2],
    explanation: "An exposure documents a downstream use (dashboard, notebook, application, ml, analysis). `name`, `type` and `owner` are required; `url`, `description`, `maturity`, `depends_on` are optional but recommended — `depends_on` is what makes the exposure appear in the DAG and enables `dbt run --select +exposure:my_dashboard`.",
    source: "https://docs.getdbt.com/docs/build/exposures",
    fr: {
      question: "Quelles propriétés sont **obligatoires** pour déclarer une exposure ? (3 réponses)",
      choices: ["name", "type", "owner (name ou email)", "url", "depends_on"],
      explanation: "Une exposure documente un usage aval (dashboard, notebook, application, ml, analysis). `name`, `type` et `owner` sont obligatoires ; `url`, `description`, `maturity`, `depends_on` sont optionnels mais recommandés — `depends_on` est ce qui fait apparaître l'exposure dans le DAG et permet `dbt run --select +exposure:mon_dashboard`.",
    },
  },
  {
    id: "dependencies-005",
    module: "dependencies",
    topic: "dbt Mesh",
    topic_fr: "dbt Mesh",
    question: "How does a `marketing` project reference the `dim_customers` model of another project `core` in dbt Mesh?",
    choices: [
      "`ref('dim_customers')` is enough if the projects share the same warehouse",
      "`ref('core', 'dim_customers')`, provided `dim_customers` is `access: public` and `core` is declared in `dependencies.yml`",
      "By installing `core` as a package in `packages.yml`",
      "Through `source('core', 'dim_customers')`",
    ],
    answer: [1],
    explanation: "Cross-project refs use `ref('<project>', '<model>')`. The producer must expose the model as `access: public`; the consumer declares the upstream project in `dependencies.yml` (`projects: - name: core`). This feature is specific to dbt Cloud (Enterprise). Installing the project as a package also works but **rebuilds** the models instead of reading them.",
    source: "https://docs.getdbt.com/docs/mesh/govern/project-dependencies",
    fr: {
      question: "Comment un projet `marketing` référence-t-il le modèle `dim_customers` d'un autre projet `core` dans dbt Mesh ?",
      choices: [
        "`ref('dim_customers')` suffit si les projets partagent le même entrepôt",
        "`ref('core', 'dim_customers')`, à condition que `dim_customers` soit `access: public` et que `core` soit déclaré dans `dependencies.yml`",
        "En installant `core` comme package dans `packages.yml`",
        "Via `source('core', 'dim_customers')`",
      ],
      explanation: "Le cross-project ref utilise `ref('<projet>', '<modèle>')`. Le producteur doit exposer le modèle en `access: public` ; le consommateur déclare le projet amont dans `dependencies.yml` (`projects: - name: core`). Cette fonctionnalité est propre à dbt Cloud (Enterprise). Installer le projet comme package fonctionne aussi mais **reconstruit** les modèles au lieu de les lire.",
    },
  },
  {
    id: "dependencies-006",
    module: "dependencies",
    topic: "Packages",
    topic_fr: "Packages",
    question: "What is the difference between declaring a project in `packages.yml` and in `dependencies.yml`?",
    choices: [
      "None, `dependencies.yml` is the new name of `packages.yml`",
      "`packages.yml` installs code (macros, models) into `dbt_packages/`; `dependencies.yml` can additionally declare dbt Cloud **projects** whose public models are read without being rebuilt",
      "`dependencies.yml` is only for private packages",
      "`packages.yml` is reserved for dbt Cloud",
    ],
    answer: [1],
    explanation: "`dependencies.yml` accepts two sections: `packages:` (equivalent to `packages.yml`, but **without** Jinja rendering — so no `env_var` for a git token) and `projects:` (dbt Mesh). You cannot have both files at the same time.",
    source: "https://docs.getdbt.com/docs/build/packages#use-cases-for-dependenciesyml-vs-packagesyml",
    fr: {
      question: "Quelle est la différence entre déclarer un projet dans `packages.yml` et dans `dependencies.yml` ?",
      choices: [
        "Aucune, `dependencies.yml` est le nouveau nom de `packages.yml`",
        "`packages.yml` installe du code (macros, modèles) dans `dbt_packages/` ; `dependencies.yml` peut en plus déclarer des **projets** dbt Cloud dont on lit les modèles publics sans les reconstruire",
        "`dependencies.yml` ne sert qu'aux packages privés",
        "`packages.yml` est réservé à dbt Cloud",
      ],
      explanation: "`dependencies.yml` accepte deux sections : `packages:` (équivalent à `packages.yml`, mais **sans** rendu Jinja — donc pas de `env_var` pour un token git) et `projects:` (dbt Mesh). On ne peut pas avoir les deux fichiers en même temps.",
    },
  },
  {
    id: "dependencies-007",
    module: "dependencies",
    topic: "Sources",
    topic_fr: "Sources",
    question: "You have a source whose database differs between dev and prod. Which approach is recommended?",
    choices: [
      "Two `sources.yml` files, one per environment",
      "`database: \"{{ 'RAW_PROD' if target.name == 'prod' else 'RAW_DEV' }}\"` (or `env_var`) in the source declaration",
      "Rename the source at every deployment",
      "Use `dbt_project.yml` → `sources: database:`",
    ],
    answer: [1],
    explanation: "A source's `database`/`schema` properties accept Jinja: you can branch on `target.name` or read `env_var()`. This keeps a single source definition and avoids drift between environments.",
    source: "https://docs.getdbt.com/docs/build/sources",
    fr: {
      question: "Vous avez une source dont la base de données diffère entre dev et prod. Quelle approche est recommandée ?",
      choices: [
        "Deux fichiers `sources.yml`, un par environnement",
        "`database: \"{{ 'RAW_PROD' if target.name == 'prod' else 'RAW_DEV' }}\"` (ou `env_var`) dans la déclaration de source",
        "Renommer la source à chaque déploiement",
        "Utiliser `dbt_project.yml` → `sources: database:`",
      ],
      explanation: "Les propriétés `database`/`schema` d'une source acceptent du Jinja : on peut brancher sur `target.name` ou lire `env_var()`. Cela garde une seule définition de source et évite les divergences entre environnements.",
    },
  },
]);
