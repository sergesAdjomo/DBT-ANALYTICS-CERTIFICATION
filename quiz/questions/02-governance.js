// Module: Model governance (contracts, access, groups, versions)
QuizBank.add([
  {
    id: "governance-001",
    module: "governance",
    topic: "Contracts",
    topic_fr: "Contracts",
    question: "A model has `contract: {enforced: true}`. Its query returns an `amount` column as `float` while the YAML declares `data_type: numeric(18,2)`. What happens on `dbt run`?",
    choices: [
      "dbt automatically casts the column to the declared type",
      "dbt raises an error before materializing the model and creates nothing",
      "dbt creates the table and emits a warning",
      "dbt creates the table with the actual type and updates the YAML",
    ],
    answer: [1],
    explanation: "With an enforced contract, dbt compares the columns and types returned by the query with the declared ones **before** materializing (through a preflight query). Any difference (missing column, extra column, different type) fails the model. The contract protects downstream consumers.",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-contracts",
    fr: {
      question: "Un modèle a `contract: {enforced: true}`. Sa requête renvoie une colonne `amount` en `float` alors que le YAML déclare `data_type: numeric(18,2)`. Que se passe-t-il au `dbt run` ?",
      choices: [
        "dbt caste automatiquement la colonne au type déclaré",
        "dbt lève une erreur avant de matérialiser le modèle et ne crée rien",
        "dbt crée la table et émet un warning",
        "dbt crée la table avec le type réel et met à jour le YAML",
      ],
      explanation: "Avec un contrat enforced, dbt compare les colonnes et types renvoyés par la requête à ceux déclarés **avant** de matérialiser (via une requête de pré-vol). Toute différence (colonne manquante, en trop, type différent) fait échouer le modèle. Le contrat protège les consommateurs aval.",
    },
  },
  {
    id: "governance-002",
    module: "governance",
    topic: "Contracts",
    topic_fr: "Contracts",
    question: "Which condition must an **incremental** model meet to enable a contract?",
    choices: [
      "Have a `unique_key`",
      "Use `on_schema_change: append_new_columns` or `fail`",
      "Use the `merge` strategy",
      "No particular condition",
    ],
    answer: [1],
    explanation: "Contracts are supported for `table`, `view` and `incremental` materializations — for the latter, only with `on_schema_change: append_new_columns` or `fail`. They are not supported for `ephemeral` models nor for Python models.",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-contracts#where-are-contracts-supported",
    fr: {
      question: "Quelle condition doit remplir un modèle **incrémental** pour pouvoir activer un contrat ?",
      choices: [
        "Avoir un `unique_key`",
        "Utiliser `on_schema_change: append_new_columns` ou `fail`",
        "Utiliser la stratégie `merge`",
        "Aucune condition particulière",
      ],
      explanation: "Les contrats sont supportés pour les matérialisations `table`, `view` et `incremental` — pour ce dernier, uniquement avec `on_schema_change: append_new_columns` ou `fail`. Ils ne sont pas supportés pour `ephemeral` ni pour les modèles Python.",
    },
  },
  {
    id: "governance-003",
    module: "governance",
    topic: "Contracts",
    topic_fr: "Contracts",
    question: "On Snowflake, which constraints declared in a contract are actually **enforced** by the platform?",
    choices: [
      "`not_null`, `unique` and `primary_key`",
      "Only `not_null`",
      "All of them, including `check`",
      "None, they are documentation only",
    ],
    answer: [1],
    explanation: "Snowflake only enforces `not_null`. `primary_key`, `unique` and `foreign_key` are created as metadata but not checked; `check` is not supported. To guarantee uniqueness, you need a `unique` test.",
    source: "https://docs.getdbt.com/reference/resource-properties/constraints",
    fr: {
      question: "Sur Snowflake, quelles contraintes déclarées dans un contrat sont réellement **appliquées** par la plateforme ?",
      choices: [
        "`not_null`, `unique` et `primary_key`",
        "Uniquement `not_null`",
        "Toutes, y compris `check`",
        "Aucune, elles sont seulement documentaires",
      ],
      explanation: "Snowflake n'applique (enforce) que `not_null`. `primary_key`, `unique` et `foreign_key` sont créées comme métadonnées mais pas vérifiées ; `check` n'est pas supporté. Pour garantir l'unicité, il faut un test `unique`.",
    },
  },
  {
    id: "governance-004",
    module: "governance",
    topic: "Access & groups",
    topic_fr: "Access & groups",
    question: "What is the default value of a model's `access` property?",
    choices: ["private", "protected", "public", "internal"],
    answer: [1],
    explanation: "By default a model is `protected`: it can be referenced by any model in the same project, but not from another project. `private` restricts references to models in the same `group`. `public` allows cross-project references (dbt Mesh).",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-access",
    fr: {
      question: "Quelle est la valeur par défaut de la propriété `access` d'un modèle ?",
      choices: ["private", "protected", "public", "internal"],
      explanation: "Par défaut un modèle est `protected` : référençable par tout modèle du même projet, mais pas depuis un autre projet. `private` restreint aux modèles du même `group`. `public` autorise la référence cross-project (dbt Mesh).",
    },
  },
  {
    id: "governance-005",
    module: "governance",
    topic: "Access & groups",
    topic_fr: "Access & groups",
    question: "A model `int_orders` has `access: private` and `group: finance`. Who can `ref('int_orders')`?",
    choices: [
      "Any model in the project",
      "Only models belonging to the `finance` group",
      "Only models in the same folder",
      "Only models that also have `access: private`",
    ],
    answer: [1],
    explanation: "`private` limits references to models in the same `group`. A `ref()` from another group (or from no group) raises a parsing error: `Node … attempted to reference node …, which is not allowed because the referenced node is private to the 'finance' group`.",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-access",
    fr: {
      question: "Un modèle `int_orders` a `access: private` et `group: finance`. Qui peut faire `ref('int_orders')` ?",
      choices: [
        "Tout modèle du projet",
        "Uniquement les modèles appartenant au groupe `finance`",
        "Uniquement les modèles du même dossier",
        "Uniquement les modèles ayant `access: private`",
      ],
      explanation: "`private` limite la référence aux modèles du même `group`. Un `ref()` depuis un autre groupe (ou sans groupe) provoque une erreur au parsing : `Node … attempted to reference node …, which is not allowed because the referenced node is private to the 'finance' group`.",
    },
  },
  {
    id: "governance-006",
    module: "governance",
    topic: "Access & groups",
    topic_fr: "Access & groups",
    question: "Which property is mandatory when defining a `group`?",
    choices: ["`description`", "`owner` (with at least `name` or `email`)", "`models`", "`access`"],
    answer: [1],
    explanation: "A group is defined in YAML under `groups:` and requires an `owner` with `name` and/or `email` (plus any free-form properties). Models are then attached through the `group:` config.",
    source: "https://docs.getdbt.com/docs/build/groups",
    fr: {
      question: "Quelle propriété est obligatoire lorsqu'on définit un `group` ?",
      choices: ["`description`", "`owner` (avec au moins `name` ou `email`)", "`models`", "`access`"],
      explanation: "Un groupe se définit dans un YAML avec `groups:` et exige un `owner` comportant `name` et/ou `email` (plus d'éventuelles propriétés libres). Les modèles sont rattachés ensuite via la config `group:`.",
    },
  },
  {
    id: "governance-007",
    module: "governance",
    topic: "Model versions",
    topic_fr: "Versions de modèles",
    question: "A model `dim_customers` declares versions 1, 2 and 3, with `latest_version: 2`. Which version does `ref('dim_customers')` resolve to without an argument?",
    choices: ["v1", "v2", "v3", "Error: ambiguous version"],
    answer: [1],
    explanation: "An unpinned `ref` resolves to `latest_version` — not necessarily the highest version number. That allows publishing a v3 as a pre-release. To target a specific version: `ref('dim_customers', v=3)`. If `latest_version` is not set, dbt takes the highest version.",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-versions",
    fr: {
      question: "Un modèle `dim_customers` a les versions 1, 2 et 3 déclarées, avec `latest_version: 2`. Vers quelle version résout `ref('dim_customers')` sans argument ?",
      choices: ["v1", "v2", "v3", "Erreur : version ambiguë"],
      explanation: "Un `ref` non épinglé résout vers `latest_version` — pas forcément la version au numéro le plus élevé. Cela permet de publier une v3 en pré-release. Pour cibler une version précise : `ref('dim_customers', v=3)`. Si `latest_version` n'est pas défini, dbt prend la version la plus élevée.",
    },
  },
  {
    id: "governance-008",
    module: "governance",
    topic: "Model versions",
    topic_fr: "Versions de modèles",
    question: "What does dbt do when a model references a version whose `deprecation_date` has passed?",
    choices: [
      "It refuses to compile the project",
      "It emits a warning at the reference but continues",
      "It automatically redirects to `latest_version`",
      "Nothing, `deprecation_date` is documentation only",
    ],
    answer: [1],
    explanation: "`deprecation_date` triggers warnings: one for the deprecated model itself, one for every `ref` that points to it. Nothing blocks — it is a communication mechanism to give consumers time to migrate.",
    source: "https://docs.getdbt.com/reference/resource-properties/deprecation_date",
    fr: {
      question: "Que fait dbt quand un modèle référence une version dont la `deprecation_date` est passée ?",
      choices: [
        "Il refuse de compiler le projet",
        "Il émet un warning à la référence mais continue",
        "Il redirige automatiquement vers `latest_version`",
        "Rien, `deprecation_date` est purement documentaire",
      ],
      explanation: "`deprecation_date` déclenche des warnings : un pour le modèle déprécié lui-même, un pour chaque `ref` qui pointe dessus. Rien ne bloque — c'est un mécanisme de communication pour laisser aux consommateurs le temps de migrer.",
    },
  },
  {
    id: "governance-009",
    module: "governance",
    topic: "Model versions",
    topic_fr: "Versions de modèles",
    question: "How do you declare that v2 of `dim_customers` is identical to v1 except that it **removes** the `legacy_id` column?",
    choices: [
      "Redeclare all columns in v2",
      "Under `versions:`, for `v: 2`, use `columns: - include: all` then `exclude: [legacy_id]`",
      "Add `deprecated: true` to the column",
      "Impossible: a new version must be a separate SQL file with no column inheritance",
    ],
    answer: [1],
    explanation: "Versions inherit the columns of the base model. `include: all` + `exclude: [...]` (or `include: [...]`) describes the differences without redeclaring everything. By default dbt looks for the file `dim_customers_v2.sql`; `defined_in` lets you name it differently.",
    source: "https://docs.getdbt.com/docs/mesh/govern/model-versions",
    fr: {
      question: "Comment déclarer que la v2 de `dim_customers` est identique à la v1 sauf qu'elle **supprime** la colonne `legacy_id` ?",
      choices: [
        "Redéclarer toutes les colonnes dans la v2",
        "Dans `versions:`, pour `v: 2`, utiliser `columns: - include: all` puis `exclude: [legacy_id]`",
        "Ajouter `deprecated: true` à la colonne",
        "Impossible : une nouvelle version doit être un fichier SQL distinct sans héritage de colonnes",
      ],
      explanation: "Les versions héritent des colonnes du modèle de base. `include: all` + `exclude: [...]` (ou `include: [...]`) permet de décrire les différences sans tout redéclarer. Par défaut, dbt cherche le fichier `dim_customers_v2.sql` ; `defined_in` permet de nommer autrement.",
    },
  },
]);
