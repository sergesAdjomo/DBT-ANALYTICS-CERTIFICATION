// Module: Managing data pipelines (commands, node selection, execution)
QuizBank.add([
  {
    id: "pipelines-001",
    module: "pipelines",
    topic: "dbt build",
    topic_fr: "dbt build",
    question: "During a `dbt build`, a `not_null` test on `stg_orders` fails (severity error). What happens to `fct_orders`, which depends on `stg_orders`?",
    choices: [
      "It runs anyway, tests do not affect models",
      "It is **skipped**: dbt does not build nodes downstream of a failing test",
      "It runs but its own tests are ignored",
      "The whole run is aborted immediately",
    ],
    answer: [1],
    explanation: "`dbt build` runs seeds, snapshots, models and tests in DAG order. A failing test on a node causes downstream nodes to be **skipped**: invalid data is not propagated. With separate `dbt run` then `dbt test`, that protection does not exist.",
    source: "https://docs.getdbt.com/reference/commands/build",
    fr: {
      question: "Lors d'un `dbt build`, un test `not_null` sur `stg_orders` échoue (sévérité error). Que se passe-t-il pour `fct_orders`, qui dépend de `stg_orders` ?",
      choices: [
        "Il est exécuté quand même, les tests n'influencent pas les modèles",
        "Il est **skipped** : dbt ne construit pas les nœuds aval d'un test en erreur",
        "Il est exécuté mais ses propres tests sont ignorés",
        "Tout le run est annulé immédiatement",
      ],
      explanation: "`dbt build` exécute seeds, snapshots, modèles et tests dans l'ordre du DAG. Un test en erreur sur un nœud provoque le **skip** des nœuds aval : on ne propage pas des données invalides. Avec `dbt run` puis `dbt test` séparés, cette protection n'existe pas.",
    },
  },
  {
    id: "pipelines-002",
    module: "pipelines",
    topic: "Node selection",
    topic_fr: "Sélection de nœuds",
    question: "What does `dbt run --select +fct_orders` select?",
    choices: [
      "`fct_orders` and all its descendants",
      "`fct_orders` and all its ancestors",
      "Only the direct parents of `fct_orders`",
      "`fct_orders`, its descendants and the ancestors of its descendants",
    ],
    answer: [1],
    explanation: "`+model` = the model and its ancestors (upstream). `model+` = the model and its descendants. `+model+` = both. `2+model` limits to 2 levels. `@model` = the model, its descendants and **all ancestors of those descendants** (useful in CI to get everything needed).",
    source: "https://docs.getdbt.com/reference/node-selection/graph-operators",
    fr: {
      question: "Que sélectionne `dbt run --select +fct_orders` ?",
      choices: [
        "`fct_orders` et tous ses descendants",
        "`fct_orders` et tous ses ancêtres",
        "Uniquement les parents directs de `fct_orders`",
        "`fct_orders`, ses descendants et les ancêtres de ses descendants",
      ],
      explanation: "`+model` = le modèle et ses ancêtres (upstream). `model+` = le modèle et ses descendants. `+model+` = les deux. `2+model` limite à 2 niveaux. `@model` = le modèle, ses descendants et **tous les ancêtres de ces descendants** (utile en CI pour tout ce dont on a besoin).",
    },
  },
  {
    id: "pipelines-003",
    module: "pipelines",
    topic: "Node selection",
    topic_fr: "Sélection de nœuds",
    question: "What is the difference between `--select \"tag:daily,config.materialized:incremental\"` and `--select \"tag:daily config.materialized:incremental\"`?",
    choices: [
      "None, comma and space are equivalent",
      "The comma is an **intersection** (both criteria), the space a **union** (either one)",
      "The comma is a union, the space an intersection",
      "Spaces are not allowed in `--select`",
    ],
    answer: [1],
    explanation: "Space = union: nodes matching at least one criterion. Comma (no spaces) = intersection: nodes matching all criteria. They can be combined: `\"tag:daily,+fct_orders\"`.",
    source: "https://docs.getdbt.com/reference/node-selection/set-operators",
    fr: {
      question: "Quelle est la différence entre `--select \"tag:daily,config.materialized:incremental\"` et `--select \"tag:daily config.materialized:incremental\"` ?",
      choices: [
        "Aucune, virgule et espace sont équivalents",
        "La virgule est une **intersection** (les deux critères), l'espace une **union** (l'un ou l'autre)",
        "La virgule est une union, l'espace une intersection",
        "L'espace n'est pas autorisé dans `--select`",
      ],
      explanation: "Espace = union : nœuds qui matchent au moins un critère. Virgule (sans espace) = intersection : nœuds qui matchent tous les critères. On peut combiner : `\"tag:daily,+fct_orders\"`.",
    },
  },
  {
    id: "pipelines-004",
    module: "pipelines",
    topic: "Node selection",
    topic_fr: "Sélection de nœuds",
    question: "Which selection method selects all nodes in a folder, e.g. `models/marts/finance`?",
    choices: ["`--select folder:marts/finance`", "`--select path:models/marts/finance`", "`--select dir:marts.finance`", "`--select models.marts.finance` only"],
    answer: [1],
    explanation: "`path:` selects by file/folder path. The shorthand `--select marts.finance` (dotted notation from `models/`) also works. Other useful methods: `tag:`, `source:`, `resource_type:`, `config.*:`, `test_type:`, `package:`, `state:`, `result:`, `exposure:`.",
    source: "https://docs.getdbt.com/reference/node-selection/methods",
    fr: {
      question: "Quelle méthode de sélection permet de choisir tous les nœuds d'un dossier, par ex. `models/marts/finance` ?",
      choices: ["`--select folder:marts/finance`", "`--select path:models/marts/finance`", "`--select dir:marts.finance`", "`--select models.marts.finance` uniquement"],
      explanation: "`path:` sélectionne par chemin de fichier/dossier. Le raccourci `--select marts.finance` (notation pointée depuis `models/`) fonctionne aussi. Autres méthodes utiles : `tag:`, `source:`, `resource_type:`, `config.*:`, `test_type:`, `package:`, `state:`, `result:`, `exposure:`.",
    },
  },
  {
    id: "pipelines-005",
    module: "pipelines",
    topic: "Node selection",
    topic_fr: "Sélection de nœuds",
    question: "You often run the same complex selection. Which mechanism lets you name and reuse it (`dbt run --selector nightly`)?",
    choices: [
      "A shell alias",
      "A `selectors.yml` file at the project root defining YAML selectors",
      "A `selector()` macro",
      "The `default_selector` config in `profiles.yml`",
    ],
    answer: [1],
    explanation: "`selectors.yml` defines named selectors with YAML syntax (`union`, `intersection`, `exclude`, `method`/`value`). A selector can be marked `default: true`. It is more readable and versionable than long `--select` strings.",
    source: "https://docs.getdbt.com/reference/node-selection/yaml-selectors",
    fr: {
      question: "Vous lancez souvent la même sélection complexe. Quel mécanisme permet de la nommer et de la réutiliser (`dbt run --selector nightly`) ?",
      choices: [
        "Un alias shell",
        "Un fichier `selectors.yml` à la racine du projet définissant des sélecteurs YAML",
        "Une macro `selector()`",
        "La config `default_selector` dans `profiles.yml`",
      ],
      explanation: "`selectors.yml` définit des sélecteurs nommés avec la syntaxe YAML (`union`, `intersection`, `exclude`, `method`/`value`). Un sélecteur peut être marqué `default: true`. C'est plus lisible et versionnable que de longues chaînes `--select`.",
    },
  },
  {
    id: "pipelines-006",
    module: "pipelines",
    topic: "Commands",
    topic_fr: "Commandes",
    question: "What does `dbt retry` do?",
    choices: [
      "Re-runs the entire last command",
      "Re-runs only the nodes that errored or were skipped in the last invocation, reading `run_results.json`",
      "Re-runs the tests that failed",
      "Re-runs the last command with `--full-refresh`",
    ],
    answer: [1],
    explanation: "`dbt retry` reads `target/run_results.json` (or `--state`) and re-executes the same command limited to nodes with status `error`, `fail` or `skipped`. Successful nodes are not replayed. It is only useful after a partially failed execution.",
    source: "https://docs.getdbt.com/reference/commands/retry",
    fr: {
      question: "Que fait `dbt retry` ?",
      choices: [
        "Relance l'intégralité de la dernière commande",
        "Relance uniquement les nœuds en erreur ou skipped de la dernière invocation, en lisant `run_results.json`",
        "Relance les tests qui ont échoué",
        "Relance la dernière commande avec `--full-refresh`",
      ],
      explanation: "`dbt retry` lit `target/run_results.json` (ou `--state`) et ré-exécute la même commande en se limitant aux nœuds `error`, `fail` ou `skipped`. Les nœuds réussis ne sont pas rejoués. Ce n'est utile qu'après une exécution partiellement échouée.",
    },
  },
  {
    id: "pipelines-007",
    module: "pipelines",
    topic: "Incremental models",
    topic_fr: "Modèles incrémentaux",
    question: "What effects does `dbt run --full-refresh` have? (choose 2)",
    choices: [
      "Incremental models are rebuilt from scratch (drop + create)",
      "Seeds are reloaded",
      "Views are recreated with `create or replace`",
      "Snapshots are reset",
    ],
    answer: [0, 2],
    explanation: "`--full-refresh` forces `is_incremental()` to `False`: incremental tables are rebuilt. Views and tables are recreated on every run anyway. Seeds are reloaded by `dbt seed --full-refresh` (useful when the CSV columns change). Snapshots are never reset by this flag — that would lose the history.",
    source: "https://docs.getdbt.com/reference/resource-configs/full_refresh",
    fr: {
      question: "Quels effets a `dbt run --full-refresh` ? (2 réponses)",
      choices: [
        "Les modèles incrémentaux sont reconstruits entièrement (drop + create)",
        "Les seeds sont rechargés",
        "Les vues sont recréées avec `create or replace`",
        "Les snapshots sont réinitialisés",
      ],
      explanation: "`--full-refresh` force `is_incremental()` à `False` : les tables incrémentales sont reconstruites. Les vues et tables sont de toute façon recréées à chaque run. Les seeds sont rechargés par `dbt seed --full-refresh` (utile quand les colonnes du CSV changent). Les snapshots ne sont jamais réinitialisés par ce flag — ce serait perdre l'historique.",
    },
  },
  {
    id: "pipelines-008",
    module: "pipelines",
    topic: "Execution",
    topic_fr: "Exécution",
    question: "What is the `threads` config in the profile for?",
    choices: [
      "The number of queries each model can run",
      "The maximum number of nodes dbt executes **in parallel** when the DAG allows it",
      "The number of connections opened for `dbt debug`",
      "The number of retries on network errors",
    ],
    answer: [1],
    explanation: "`threads` sets the parallelism: dbt launches up to N independent nodes simultaneously. It only matters when the DAG has parallel branches, and it consumes more warehouse resources. Typical value: 4 to 16.",
    source: "https://docs.getdbt.com/docs/running-a-dbt-project/using-threads",
    fr: {
      question: "À quoi sert la config `threads` du profil ?",
      choices: [
        "Au nombre de requêtes que chaque modèle peut exécuter",
        "Au nombre maximum de nœuds que dbt exécute **en parallèle** lorsque le DAG le permet",
        "Au nombre de connexions ouvertes pour `dbt debug`",
        "Au nombre de retries en cas d'erreur réseau",
      ],
      explanation: "`threads` fixe le parallélisme : dbt lance jusqu'à N nœuds indépendants simultanément. Ça n'a d'effet que si le DAG a des branches parallèles, et ça consomme plus de ressources sur l'entrepôt. Valeur typique : 4 à 16.",
    },
  },
  {
    id: "pipelines-009",
    module: "pipelines",
    topic: "Commands",
    topic_fr: "Commandes",
    question: "What does the `--empty` flag do (`dbt run --empty`)?",
    choices: [
      "Deletes the content of the tables before the run",
      "Runs the models adding `limit 0` to `ref()`/`source()`, to validate the SQL and create empty objects at minimal cost",
      "Ignores models with no rows",
      "Only compiles, without executing",
    ],
    answer: [1],
    explanation: "`--empty` (dbt 1.8+) runs the models with empty inputs: the objects are created, the SQL is validated by the warehouse, but no data is processed. Useful in CI for a \"schema-only build\", or to prepare parents before unit tests.",
    source: "https://docs.getdbt.com/reference/commands/run#the---empty-flag",
    fr: {
      question: "Que fait le flag `--empty` (`dbt run --empty`) ?",
      choices: [
        "Supprime le contenu des tables avant le run",
        "Exécute les modèles en ajoutant `limit 0` aux `ref()`/`source()`, pour valider le SQL et créer les objets vides à moindre coût",
        "Ignore les modèles sans lignes",
        "Ne fait que compiler, sans exécuter",
      ],
      explanation: "`--empty` (dbt 1.8+) fait tourner les modèles avec des entrées vides : les objets sont créés, le SQL est validé par l'entrepôt, mais aucune donnée n'est traitée. Utile en CI pour un « schema-only build », ou pour préparer les parents avant des unit tests.",
    },
  },
  {
    id: "pipelines-010",
    module: "pipelines",
    topic: "Environments",
    topic_fr: "Environnements",
    question: "In `profiles.yml`, how do you avoid writing a password in clear text?",
    choices: [
      "Put it in `dbt_project.yml` under `vars`",
      "Use `\"{{ env_var('DBT_PASSWORD') }}\"` and set the environment variable",
      "Encrypt it with `dbt encrypt`",
      "Write it in `.dbt/secrets.yml`",
    ],
    answer: [1],
    explanation: "`env_var()` is available in `profiles.yml` and `dbt_project.yml`. Variables prefixed `DBT_ENV_SECRET_` are additionally masked in the logs. That is what this project's CI workflow does with `DBT_SNOWFLAKE_USER`, etc.",
    source: "https://docs.getdbt.com/reference/dbt-jinja-functions/env_var",
    fr: {
      question: "Dans `profiles.yml`, comment éviter d'écrire un mot de passe en clair ?",
      choices: [
        "Le mettre dans `dbt_project.yml` sous `vars`",
        "Utiliser `\"{{ env_var('DBT_PASSWORD') }}\"` et définir la variable d'environnement",
        "Le chiffrer avec `dbt encrypt`",
        "L'écrire dans `.dbt/secrets.yml`",
      ],
      explanation: "`env_var()` est disponible dans `profiles.yml` et `dbt_project.yml`. Les variables préfixées `DBT_ENV_SECRET_` sont en plus masquées dans les logs. C'est ce que fait le workflow CI de ce projet avec `DBT_SNOWFLAKE_USER`, etc.",
    },
  },
]);
