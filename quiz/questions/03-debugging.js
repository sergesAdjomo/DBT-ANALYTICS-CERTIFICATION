// Module: Debugging data modeling errors
QuizBank.add([
  {
    id: "debugging-001",
    module: "debugging",
    topic: "target/ artifacts",
    topic_fr: "Artefacts target/",
    question: "You want to read the SQL **exactly as sent to the warehouse** for an incremental model (with the `merge` and the DDL). Where do you find it?",
    choices: ["target/compiled/", "target/run/", "target/manifest.json", "logs/dbt.log only"],
    answer: [1],
    explanation: "`target/compiled/` holds the SQL after Jinja rendering but **without** the materialization wrapper (just the `select`). `target/run/` holds the SQL actually executed: `create table as`, `merge`, etc. `dbt.log` contains it too, but buried in the logs.",
    source: "https://docs.getdbt.com/reference/artifacts/dbt-artifacts",
    fr: {
      question: "Vous voulez lire le SQL **exactement tel qu'envoyé à l'entrepôt** pour un modèle incrémental (avec le `merge` et les DDL). Où le trouver ?",
      choices: ["target/compiled/", "target/run/", "target/manifest.json", "logs/dbt.log uniquement"],
      explanation: "`target/compiled/` contient le SQL après rendu Jinja mais **sans** l'enrobage de matérialisation (juste le `select`). `target/run/` contient le SQL réellement exécuté : `create table as`, `merge`, etc. `dbt.log` le contient aussi, mais noyé dans les logs.",
    },
  },
  {
    id: "debugging-002",
    module: "debugging",
    topic: "Commands",
    topic_fr: "Commandes",
    question: "Which command checks the warehouse connection and the validity of `profiles.yml` and `dbt_project.yml`?",
    choices: ["dbt parse", "dbt debug", "dbt compile", "dbt test --connection"],
    answer: [1],
    explanation: "`dbt debug` validates the config (profile, project, dependencies) and tests the connection. `dbt parse` checks that the project parses (DAG, YAML) without connecting. `dbt compile` renders the SQL and may need the warehouse for introspective queries.",
    source: "https://docs.getdbt.com/reference/commands/debug",
    fr: {
      question: "Quelle commande vérifie la connexion à l'entrepôt, la validité de `profiles.yml` et de `dbt_project.yml` ?",
      choices: ["dbt parse", "dbt debug", "dbt compile", "dbt test --connection"],
      explanation: "`dbt debug` valide la config (profil, projet, dépendances) et teste la connexion. `dbt parse` vérifie que le projet parse (DAG, YAML) sans se connecter. `dbt compile` rend le SQL et peut avoir besoin de l'entrepôt pour les requêtes introspectives.",
    },
  },
  {
    id: "debugging-003",
    module: "debugging",
    topic: "Common errors",
    topic_fr: "Erreurs courantes",
    question: "Message: `Compilation Error in model fct_orders: Model 'model.eth.fct_orders' depends on a node named 'stg_order' which was not found`. What is the most likely cause?",
    choices: [
      "The `stg_order` table does not exist in the warehouse",
      "The `ref('stg_order')` points to a non-existent or disabled model (typo, `enabled: false`)",
      "The user lacks privileges on the schema",
      "The `stg_order` model failed in the previous run",
    ],
    answer: [1],
    explanation: "This is a **parsing/compilation** error, not an execution error: dbt cannot find a node named `stg_order` in the project. Typical causes: typo (`stg_orders`), disabled model, file outside `model-paths`, or package not installed (`dbt deps`).",
    source: "https://docs.getdbt.com/guides/debug-errors",
    fr: {
      question: "Message : `Compilation Error in model fct_orders: Model 'model.eth.fct_orders' depends on a node named 'stg_order' which was not found`. Quelle est la cause la plus probable ?",
      choices: [
        "La table `stg_order` n'existe pas dans l'entrepôt",
        "Le `ref('stg_order')` pointe vers un modèle inexistant ou désactivé (faute de frappe, `enabled: false`)",
        "L'utilisateur n'a pas les droits sur le schéma",
        "Le modèle `stg_order` a échoué au run précédent",
      ],
      explanation: "C'est une erreur de **parsing/compilation**, pas d'exécution : dbt ne trouve pas de nœud nommé `stg_order` dans le projet. Causes typiques : faute de frappe (`stg_orders`), modèle désactivé, fichier hors des `model-paths`, ou package non installé (`dbt deps`).",
    },
  },
  {
    id: "debugging-004",
    module: "debugging",
    topic: "Common errors",
    topic_fr: "Erreurs courantes",
    question: "A `dbt run` fails with `Database Error … Object 'RAW.ETH.CONTRACTS' does not exist or not authorized`. At which stage does the error occur and what does it mean?",
    choices: [
      "At parsing: the source YAML is malformed",
      "At execution: the SQL is valid but the warehouse cannot find the object or denies access",
      "At compilation: Jinja could not render the `source()`",
      "At `dbt deps`: the package is missing",
    ],
    answer: [1],
    explanation: "A `Database Error` comes from the warehouse, so after compilation: the SQL was sent and Snowflake answered. Check `database`/`schema` in `sources.yml`, the role used in the profile, and the grants. Parsing/compilation errors are prefixed `Compilation Error` or `Parsing Error`.",
    source: "https://docs.getdbt.com/guides/debug-errors",
    fr: {
      question: "Un `dbt run` échoue avec `Database Error … Object 'RAW.ETH.CONTRACTS' does not exist or not authorized`. À quelle étape l'erreur survient-elle et que signifie-t-elle ?",
      choices: [
        "Au parsing : le YAML de source est mal formé",
        "À l'exécution : le SQL est valide mais l'entrepôt ne trouve pas l'objet ou refuse l'accès",
        "À la compilation : Jinja n'a pas pu rendre le `source()`",
        "Au `dbt deps` : le package est absent",
      ],
      explanation: "Un `Database Error` vient de l'entrepôt, donc après compilation : le SQL a été envoyé et Snowflake a répondu. Il faut vérifier `database`/`schema` dans `sources.yml`, le rôle utilisé dans le profil, et les grants. Les erreurs de parsing/compilation sont préfixées `Compilation Error` ou `Parsing Error`.",
    },
  },
  {
    id: "debugging-005",
    module: "debugging",
    topic: "Logs",
    topic_fr: "Logs",
    question: "Where does dbt write the detailed logs (SQL queries sent, timings, stack traces) of a run?",
    choices: ["target/run_results.json", "logs/dbt.log", "target/logs.txt", "profiles.yml"],
    answer: [1],
    explanation: "`logs/dbt.log` contains debug-level logs for every invocation. `run_results.json` only contains the per-node summary (status, duration, message). The `--debug` (`-d`) flag prints that level of detail in the terminal.",
    source: "https://docs.getdbt.com/reference/global-configs/logs",
    fr: {
      question: "Où dbt écrit-il les logs détaillés (requêtes SQL envoyées, timings, stack traces) d'un run ?",
      choices: ["target/run_results.json", "logs/dbt.log", "target/logs.txt", "profiles.yml"],
      explanation: "`logs/dbt.log` contient les logs de niveau debug de chaque invocation. `run_results.json` contient uniquement le résumé par nœud (statut, durée, message). Le flag `--debug` (`-d`) affiche ce niveau de détail dans le terminal.",
    },
  },
  {
    id: "debugging-006",
    module: "debugging",
    topic: "Commands",
    topic_fr: "Commandes",
    question: "You want to check that a model compiles correctly and see its rendered SQL **without** creating or modifying any object in the warehouse. Which command?",
    choices: ["dbt run --dry-run", "dbt compile --select my_model", "dbt show --select my_model", "dbt build --empty"],
    answer: [1],
    explanation: "`dbt compile` renders the Jinja and writes the SQL to `target/compiled/` without materializing. `dbt show` compiles **and runs** a (limited) preview of the query. `--empty` runs the models with `limit 0` (it does create the objects, empty). `--dry-run` does not exist.",
    source: "https://docs.getdbt.com/reference/commands/compile",
    fr: {
      question: "Vous voulez vérifier qu'un modèle compile correctement et voir son SQL rendu **sans** créer ni modifier d'objet dans l'entrepôt. Quelle commande ?",
      choices: ["dbt run --dry-run", "dbt compile --select mon_modele", "dbt show --select mon_modele", "dbt build --empty"],
      explanation: "`dbt compile` rend le Jinja et écrit le SQL dans `target/compiled/` sans matérialiser. `dbt show` compile **et exécute** un aperçu (limité) de la requête. `--empty` exécute les modèles avec `limit 0` (crée bien les objets, vides). `--dry-run` n'existe pas.",
    },
  },
  {
    id: "debugging-007",
    module: "debugging",
    topic: "Jinja & macros",
    topic_fr: "Jinja & macros",
    question: "A model uses `{{ var('cutoff') }}` and fails with `Required var 'cutoff' not found in config`. Which fix avoids the error while keeping a default value?",
    choices: [
      "`{{ var('cutoff') or '2024-01-01' }}`",
      "`{{ var('cutoff', '2024-01-01') }}`",
      "`{{ env_var('cutoff') }}`",
      "`{% set cutoff = '2024-01-01' %}` before the call",
    ],
    answer: [1],
    explanation: "`var(name, default)` returns `default` when the variable is not defined. Without a default, a missing variable raises a compilation error. Option A still fails because `var('cutoff')` is evaluated before the `or`.",
    source: "https://docs.getdbt.com/reference/dbt-jinja-functions/var",
    fr: {
      question: "Un modèle utilise `{{ var('cutoff') }}` et échoue avec `Required var 'cutoff' not found in config`. Quelle correction évite l'erreur tout en gardant une valeur par défaut ?",
      choices: [
        "`{{ var('cutoff') or '2024-01-01' }}`",
        "`{{ var('cutoff', '2024-01-01') }}`",
        "`{{ env_var('cutoff') }}`",
        "`{% set cutoff = '2024-01-01' %}` avant l'appel",
      ],
      explanation: "`var(name, default)` renvoie `default` si la variable n'est pas définie. Sans défaut, une variable absente lève une erreur de compilation. L'option A échoue quand même car `var('cutoff')` est évalué avant le `or`.",
    },
  },
]);
